import { SxThemeProps } from "@shared/types/sx.types"

export const styles = {
    container: {
        maxWidth: "720px",
        margin: "auto",
        padding: "32px 0"
    },
} satisfies SxThemeProps